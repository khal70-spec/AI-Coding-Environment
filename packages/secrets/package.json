{
  "name": "@ai-coding-env/secrets",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Secret vault interface, test double, and metadata (handles, never values)",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "test": "node --test \"../../tests/unit/secrets/**/*.test.mjs\""
  }
}
