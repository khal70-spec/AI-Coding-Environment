{
  "name": "ai-coding-environment",
  "version": "0.3.0",
  "private": true,
  "description": "Secure, provider-neutral AI coding operating environment and agent orchestrator",
  "license": "Apache-2.0",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "node --test \"tests/unit/**/*.test.mjs\" \"tests/security/**/*.test.mjs\" \"tests/integration/**/*.test.mjs\"",
    "test:workspaces": "npm run test --workspaces --if-present",
    "lint": "eslint . --ext .ts,.tsx 2>/dev/null || echo \"lint: eslint flat config lands in Phase 3 — skipped (advisory)\"",
    "typecheck": "tsc --noEmit -p tsconfig.base.json",
    "audit:deps": "npm audit --audit-level=high",
    "check:secrets": "node scripts/check-secrets.mjs",
    "db:migrate": "node scripts/db-migrate.mjs"
  },
  "engines": {
    "node": ">=22.18.0"
  },
  "devDependencies": {
    "@types/node": "22.20.3",
    "typescript": "5.9.3"
  }
}
