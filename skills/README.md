# Skills (Phase 6 — placeholder)

Versioned capability packages (`skills/<name>/`) per Plan §40. Untrusted extensions:
identity, permissions manifest, validation, version, review status. No silent elevation.
